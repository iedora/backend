@inherits LayoutComponentBase

@* Chromeless layout for pre-auth pages (login) — no admin nav, but still needs the Mud providers
   so themed components (and the snackbar) work. *@
<MudProviders />

@Body
