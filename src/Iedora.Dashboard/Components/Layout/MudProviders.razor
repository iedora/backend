@* MudBlazor root providers — needed once inside every layout so components under it can theme,
   pop overs open, and dialogs/snackbars have a host. Shared by MainLayout and EmptyLayout. *@
<MudThemeProvider Theme="IedoraTheme.Instance" />
<MudPopoverProvider />
<MudDialogProvider />
<MudSnackbarProvider />
