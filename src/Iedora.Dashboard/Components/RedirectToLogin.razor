@inject NavigationManager Nav

@* Bounce an unauthenticated visitor to the login page (full load, so the cookie flow runs). *@
@code {
    protected override void OnInitialized() => Nav.NavigateTo("/login", forceLoad: true);
}
