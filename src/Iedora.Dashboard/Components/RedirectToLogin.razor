@inject NavigationManager Nav

@* Bounce an unauthenticated visitor to the login page (client-side — no full reload in the SPA). *@
@code {
    protected override void OnInitialized() => Nav.NavigateTo("/login");
}
